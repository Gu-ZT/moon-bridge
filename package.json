{
  "name": "moonbridge-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "go run github.com/syumai/workers/cmd/workers-assets-gen && node scripts/patch_wasm_exec.mjs build/wasm_exec.js && tinygo build -o ./build/app.wasm -target wasm ./cmd/cloudflare",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.86.0"
  }
}
